Write pytest tests for the Flask API.

Test these endpoints:

/health
/stadiums
/api/fixtures
/api/fixtures/weather

Use Flask test client.

Tests must verify:

- correct status codes
- JSON structure
- weather fields exist
- errors return JSON

Place tests inside tests/ directory.